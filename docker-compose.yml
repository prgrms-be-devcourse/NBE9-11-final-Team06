services:
  backend:
    container_name: gotoday-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: gotoday-backend:latest
    ports:
      - "8081:8080"
    environment:
      SPRING_PROFILES_ACTIVE: dev
    restart: unless-stopped